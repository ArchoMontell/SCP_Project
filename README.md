# SCP Foundation Database System

Захищена система управління та обліку аномальних об'єктів Фонду SCP. Проект реалізований на Flask з підтримкою ролей користувачів, класифікації об'єктів, управління підрозділами утримання та журналю подій.

**Мова інтерфейсу:** Українська 🇺🇦

---

## Основні характеристики

- **Управління користувачами** — реєстрація, вхід, ролі (адміністратор / користувач), рівні доступу (0–5)
- **Управління підрозділами утримання** — створення, редагування, видалення камер з параметрами безпеки
- **Облік аномальних об'єктів** — реєстрація, класифікація (Безпечний, Евклід, Кетер, Тауміель, тощо), прив'язка до підрозділів
- **Журнал подій** — логування інцидентів, техобслуговування, подій доступу
- **Темна тема з золотими акцентами** — SCP-орієнтований дизайн
- **Мобільна сумісність** — адаптивний інтерфейс

---

## Структура проекту

```
SCP_Project/
├── scp-backend/                  # Основний Flask додаток
│   ├── app.py                    # Фабрика додатку та маршрути
│   ├── models.py                 # ORM моделі (User, Camera, ObjectItem, Event)
│   ├── forms.py                  # WTForms для реєстрації, форм об'єктів/камер
│   ├── config.py                 # Конфігурація додатку
│   ├── scp.db                    # SQLite база даних
│   ├── static/
│   │   └── style.css             # Глобальна таблиця стилів
│   └── templates/                # Jinja2 шаблони
│       ├── base.html             # Базовий шаблон (навігація, стилі)
│       ├── index.html            # Головна сторінка
│       ├── register.html         # Форма реєстрації
│       ├── login.html            # Форма входу
│       ├── dashboard_user.html   # Панель користувача
│       ├── dashboard_admin.html  # Панель адміністратора
│       ├── add_camera.html       # Додання підрозділу
│       ├── edit_camera.html      # Редагування підрозділу
│       ├── add_object.html       # Додання об'єкта (адмін)
│       ├── edit_object.html      # Редагування об'єкта
│       ├── user_add_object.html  # Додання об'єкта (користувач)
│       ├── add_event.html        # Логування події
│       ├── view_camera.html      # Перегляд підрозділу та об'єктів
│       └── ... (інші шаблони)
├── src/                          # Статичні сторінки
│   ├── index.html                # Вступна сторінка проекту
│   └── style.css                 # Основні стилі
├── doc/                          # Документація
│   ├── 01_Research.md
│   ├── 02_Entities_and_Scenarios.md
│   └── 03_Architecture_and_Code_Overview.md
├── requirements.txt              # Python залежності
├── README.md                     # Цей файл
└── venv/                         # Віртуальне середовище Python
```

---

## Встановлення та запуск

### Вимоги
- **Python 3.8+**
- **pip** (менеджер пакетів Python)

### 1. Клонування репозиторію
```bash
git clone https://github.com/ArchoMontell/SCP_Project.git
cd SCP_Project
```

### 2. Створення віртуального середовища
```bash
# Windows
python -m venv venv
venv\Scripts\Activate.ps1

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### 3. Встановлення залежностей
```bash
pip install -r requirements.txt
```

### 4. Запуск додатку
```bash
cd scp-backend
python app.py
```

Додаток запуститься на `http://127.0.0.1:5000` в режимі розробки (debug mode).

---

## Перша конфігурація

### Створення адміністратора (опціонально)

Якщо ви хочете швидко створити облік адміністратора, запустіть команду Flask CLI:

```bash
cd scp-backend
export ADMIN_USER=admin
export ADMIN_EMAIL=admin@scp.foundation
export ADMIN_PASS=admin_password
flask create-admin
```

**Примітка:** В Windows використовуйте `set` замість `export`:
```powershell
$env:ADMIN_USER = "admin"
$env:ADMIN_EMAIL = "admin@scp.foundation"
$env:ADMIN_PASS = "admin_password"
flask create-admin
```

Після запуску ви можете увійти на сторінку `/login` і використовувати ці облікові дані.

---

## Основні маршрути

| URL | Опис | Доступ |
|-----|------|--------|
| `/` | Головна сторінка | Відкрито |
| `/register` | Реєстрація нового користувача | Публічно |
| `/login` | Вхід у систему | Публічно |
| `/logout` | Вихід із системи | Авторизовані |
| `/dashboard` | Перенаправлення на релевантну панель | Авторизовані |
| `/admin/dashboard` | Панель адміністратора | Тільки адмін |
| `/admin/camera/add` | Додання підрозділу | Тільки адмін |
| `/admin/object/add` | Додання об'єкта (адмін) | Тільки адмін |
| `/admin/event/add` | Логування події | Тільки адмін |
| `/user/dashboard` | Панель користувача | Користувачі |
| `/user/object/add` | Додання об'єкта (користувач) | Користувачі |
| `/user/camera/<id>` | Перегляд підрозділу | Користувачі |

---

## ORM Моделі

### User
```python
- id (Integer, PK)
- username (String, unique)
- email (String, unique)
- password_hash (String)
- role (String: 'admin' або 'user')
- access_level (Integer: 0–5)
- created_at (DateTime)
```

### Camera (Підрозділ утримання)
```python
- id (Integer, PK)
- name (String)
- type (String)
- max_capacity (Integer)
- current_capacity (Integer)
- security_level (Integer: 0–5)
- equipment_list (Text)
- cleaning_schedule (Text)
- maintenance_schedule (Text)
- created_at, updated_at (DateTime)
```

### ObjectItem (Аномальний об'єкт)
```python
- id (Integer, PK)
- name (String)
- classification (String: 'Safe', 'Euclid', 'Keter', 'Thaumiel', 'Exotic', 'Metaclass', 'Unspecified')
- description (Text)
- storage_requirements (Text)
- camera_id (Integer, FK)
- status (String)
- history_of_movements (Text)
- created_at, updated_at (DateTime)
```

### Event
```python
- id (Integer, PK)
- type (String)
- camera_id (Integer, FK)
- object_id (Integer, FK)
- user_id (Integer, FK)
- timestamp (DateTime)
- notes (Text)
```

---

## Конфігурація

Основні параметри конфігурації знаходяться в `scp-backend/config.py`:

```python
class Config:
    SQLALCHEMY_DATABASE_URI = 'sqlite:///scp.db'
    SQLALCHEMY_TRACK_MODIFICATIONS = False
    SECRET_KEY = os.environ.get('SECRET_KEY', 'dev-secret-key-change-in-production')
    WTF_CSRF_ENABLED = True
```

**Рекомендація:** Для продакшену встановіть безпечний `SECRET_KEY` через змінну середовища.

---

## Розробка

### Структура Flask додатку
- **app.py** — точка входу, містить `create_app()` та всі маршрути
- **models.py** — ORM моделі SQLAlchemy
- **forms.py** — WTForms для валідації
- **config.py** — конфігураційні класи

### Поточний стан
- ✅ Авторизація та управління користувачами
- ✅ CRUD операції для камер, об'єктів, подій
- ✅ Ролі та контроль доступу
- ✅ Повний переклад інтерфейсу українською мовою
- ✅ Темна тема SCP-стилю

---

## Тестування

На даний момент додаток протестований вручну через основні сценарії:
1. Реєстрація користувача
2. Вхід у систему
3. Управління підрозділами
4. Додання та редагування об'єктів
5. Логування подій

Для автоматичного тестування рекомендується впровадити `pytest` з фікстурами.

---

## Відомі обмеження

- База даних — SQLite (придатна для розробки; для продакшену рекомендується PostgreSQL/MySQL)
- Немає автоматичних міграцій (використовується `db.create_all()`)
- Вхідна валідація базична; для продакшену потрібно додати подальші перевірки

---

## Ліцензія

Цей проект є освітнім матеріалом, створеним для демонстрації навичок Flask веб-розробки.

---

## Контакти / Співпраця

Для запитань або пропозицій щодо проекту звертайтесь до автора або створіть `Issue` на GitHub.

**GitHub репозиторій:** [ArchoMontell/SCP_Project](https://github.com/ArchoMontell/SCP_Project)

---

**Посл. оновлення:** 26 листопада 2025 р.  
**Версія:** 0.2 (Simplification + Full Ukrainian Translation)
